[tools]
rust = "1.93"

[settings]
quiet = true

[tasks.docker-build]
description = "Build the Docker image"
run = "docker build -t dev-docker-mise-rust ."

[tasks.docker-rebuild]
description = "Rebuild the Docker image without cache"
run = "docker build --no-cache -t dev-docker-mise-rust ."
